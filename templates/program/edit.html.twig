{% extends 'base.html.twig' %}

{% block title %}Modifier la série
{% endblock %}

{% block body %}
    <main class="container">
        <div class="d-grid gap-2 d-md-flex mb-3 justify-content-md-end mt-5">
            <a href="{{ path('program_index') }}" class="btn btn-primary btn-sm" type="button">Retour à la liste</a>
        </div>
        <h1>Modifier la fiche : {{ program.title }}</h1>
        {{ include('program/_form.html.twig', {'button_label': 'Modifier'}) }}
        {{ include('program/_delete_form.html.twig', {'button_label': 'Supprimer'}) }}
    </main>
{% endblock %}
