{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form) }}
{{ form_widget(form) }}
<div class="d-grid gap-2 d-md-flex mb-3 justify-content-md-end">
    <button class="btn btn-sm bg-warning">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
